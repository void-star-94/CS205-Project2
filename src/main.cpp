#include "Executor.h"
#include <iostream>
#include <string_view>

int main() {
    int a = 5 * - + +5;
    std::cout << a;
    return 0;
    while (true) {
        auto status = void_star::get_input();
        if (!void_star::process_input(status))void_star::print_error(status);
    }
}
